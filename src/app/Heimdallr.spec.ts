import {Heimdallr} from './Heimdallr';

describe('AbstractMessage Component', () => {
  beforeEach(()=> {
  });

  it('should set proper defaults in constructors before init', ()=> {
    expect(true).toBe(false);
  });

});
